# CYBER SH — Python Dependencies
# Install with: pip install -r requirements.txt

# Core — AI engine (Groq cloud, free tier)
groq
fastapi
uvicorn

# Web search — enables /web and /cvesearch commands (recommended)
ddgs

supabase==2.0.0
